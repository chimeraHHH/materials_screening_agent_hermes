# Agent04 domain schema fixtures

These are small, deterministic, abstract Hubbard-model inputs for the Agent04
domain contract. They contain no solver output, observables, credentials,
machine paths, diagnostic output, or real material/DFT data. `fixture=true` and
`is_mock=true` are intentional and impose an evidence ceiling below L4.
